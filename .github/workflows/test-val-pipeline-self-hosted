---
name: Test the validation pipeline reusable workflow (self-hosted)

on:
  push:
    branches:
      - main

jobs:
  test-val-pipeline-self-hosted-aragog:
    uses: ./.github/workflows/val-pipeline-self-hosted.yaml
    with:
      target: aragog
      report_pkg_dir: ./tests/packages/aragog
